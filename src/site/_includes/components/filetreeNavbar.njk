<nav class="navbar">
    <div class="navbar-inner">
        <span style="font-size: 1.5rem; margin-right: 10px;" @click="showFilesMobile=!showFilesMobile" class="navbar-menu-icon"><i  data-lucide="menu"></i></span>
        {% for imp in dynamics.filetree.beforeTitle %}
            {% include imp %}
        {% endfor %}
        <a href="/" style="text-decoration: none; display: flex; align-items: center;">
            <h1 style="margin: 15px !important;">{{meta.siteName}}</h1>
        </a>
        {% if meta.siteLogoPath %}
            <img class="site-logo me-image" style="margin-left: auto;" src="{{meta.siteLogoPath}}" alt="{{meta.siteName}}">
        {% endif %}
        {% for imp in dynamics.filetree.afterTitle %}
            {% include imp %}
        {% endfor %}
    </div>

    {% if settings.dgEnableSearch === true%}
        {% include "components/searchButton.njk" %}
    {%endif%}
</nav>